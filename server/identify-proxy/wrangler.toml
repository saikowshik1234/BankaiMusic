name = "turntable-identify-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"
workers_dev = true
preview_urls = false

# Secrets (AUDD_API_TOKEN, PROXY_SHARED_KEY) are set via `wrangler secret put`,
# never written here — this file is safe to commit.
